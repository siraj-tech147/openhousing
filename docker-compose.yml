services:

  api:
    build: .
    container_name: openhousing-api
    ports:
      - "8000:8000"

  dashboard:
    build: .
    container_name: openhousing-dashboard
    ports:
      - "8501:8501"
    command: streamlit run dashboard/app.py --server.address=0.0.0.0
    depends_on:
      - api